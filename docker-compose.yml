version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: svoya-igra-frontend
    ports:
      - "8080:80" # Открываем сайт на http://localhost:8080
    restart: unless-stopped
