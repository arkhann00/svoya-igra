services:
  frontend-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: svoya-igra-frontend-dev
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 5173
    restart: unless-stopped
